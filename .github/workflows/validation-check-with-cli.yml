name: Zally api specification linter

on:
  workflow_call:
    inputs:
      file-path:
        required: true
        type: string

jobs:
  build-zally:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Main Repository
        uses: actions/checkout@v3
        with:
          path: main
      - name: Checkout Zally
        uses: actions/checkout@v3
        with:
          repository: cekinmezyucel/zally
          token: ${{ github.token }}
          path: zally
      - name: Set up JDK
        uses: actions/setup-java@v2
        with:
          java-version: '17'
          distribution: 'adopt'
      - name: Setup Go
        uses: actions/setup-go@v2
        with:
          go-version: '1.16'
      - name: Add Zally CLI Path
        run: echo "zally/zally/cli/zally" >> $GITHUB_PATH
      - name: Run Zally server
        run: |
          pwd
          ls
          cd zally
          ls
          cd zally
          ls
          cd server
          chmod +x gradlew
          ./gradlew --console=plain bootRun > /dev/null &
          echo $! > /tmp/zally_server.pid
          cd ..
          while ! echo "GET / HTTP/1.0\n" | nc localhost 8000; do sleep 1; done
      - name: Build CLI
        run: |
          cd zally/zally/cli/zally
          go build
      - name: Lint file
        run: zally lint ${{ inputs.file-path }}